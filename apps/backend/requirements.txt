fastapi==0.128.4
uvicorn==0.40.0
python-multipart==0.0.26
pydantic==2.12.5
pydantic-settings==2.12.0
tinydb==4.8.2
litellm==1.83.10
markitdown[docx]==0.1.4
python-docx==1.2.0
playwright==1.58.0
python-dotenv==1.0.1
# Semantic upgrade (Phase 1) — no extra deps required:
# - litellm.aembedding() handles Ollama nomic-embed-text natively
# - cosine_similarity uses pure Python math (no numpy)
# To pull the embedding model in Ollama run:
#   ollama pull nomic-embed-text
